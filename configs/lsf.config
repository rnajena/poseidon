workDir = params.workdir

executor {
    name = "lsf"
    queueSize = 100
}
            
process {   
    cache = "lenient"
    //errorStrategy = "retry"
    //maxRetries = 1
    withLabel: foo { cpus = 32 ; memory = '24 GB' } 
}
