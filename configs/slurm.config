workDir = params.workdir

executor {
    name = "slurm"
    queueSize = 100
}

process {   
    cache = "lenient"
    //errorStrategy = "retry"
    //maxRetries = 1
    withLabel: foo { cpus = 32 ; memory = '24 GB' } 
}
